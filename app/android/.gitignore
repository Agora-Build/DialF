gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
# Exception: the committed *sideload* signing key. Not a Play Store key — its only job is
# a STABLE signature so sideloaded APK updates install over each other (no uninstall).
!app/dialf-sideload.keystore
